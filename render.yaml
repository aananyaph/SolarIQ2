services:
  - type: web
    name: solariq-frontend
    env: node
    rootDir: frontend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: BACKEND_URL
        value: https://solariq-backend.vercel.app
      - key: NODE_ENV
        value: production
